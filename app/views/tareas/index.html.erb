<h1>Lista de tareas</h1>
<ul>
	<% @tareas.each do |tarea|%>
		<li> <%=link_to "#{tarea.titulo}", tarea %> - 
			<%=link_to "Eliminar", tarea, 
			method: :delete, data: {confirm: "Estas seguro?"} %> 
			- <%=link_to "editar", edit_tarea_path(tarea)%>
		</li>	
	<% end %>	
</ul>